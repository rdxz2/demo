- name: Producer - Basic
  hosts: producers
  vars_files:
    - vars/main.yaml
  roles:
    - docker
  tags:
    - basic

- name: Producer
  hosts: producers
  vars_files:
    - vars/main.yaml
    - "{{ secrets_file }}"
  roles:
    - producer
  tags:
    - producer
