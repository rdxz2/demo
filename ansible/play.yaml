- name: Airflow - Basic setup
  hosts: airflows
  vars_files:
    - vars/main.yaml
  roles:
    - setup
  tags:
    - airflow_basic

- name: Airflow - Installation
  hosts: airflows
  vars_files:
    - vars/main.yaml
    - vars/secrets.yaml
  roles:
    - airflow_install
  tags:
    - airflow_install

- name: Airflow - Configuration
  hosts: airflows
  vars_files:
    - vars/main.yaml
    - vars/secrets.yaml
  roles:
    - airflow_configure
  tags:
    - airflow_configure
